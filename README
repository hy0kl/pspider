python spider

Fetch title for new song, from qq music, kuwo, baidu, 9sky, kugou, top100, sogou, 1ting and so on.

Usage:
#cd "root-path"
./shell/control.sh start

crontab -e
0 8 * * * cd /opt/fetch-new-title && ./shell/control.sh start >> /opt/fetch-new-title/log/crontab.$(date +"%Y-%m-%d").log
